[package]
name = "supgang"
description = "Finds the current address of a computer whose address keeps changing"
publish = ["crates-io"]
categories = ["command-line-utilities", "network-programming"]
keywords = ["discovery", "peer-to-peer", "dynamic-ip", "networking"]
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "../../README.md"

[[bin]]
name = "supgang"
path = "src/main.rs"
doc = false

[dependencies]
supgang-core = { path = "../supgang", version = "=0.1.0" }

[lints]
workspace = true
